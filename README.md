# ranp

(**Ran**)dom (**p**)rint (`ranp`) is a script to send a random [xkcd](https://xkcd.com/) to a physical printer.

## Dependencies

- bash
- curl
- jq
- texlive (for xelatex engine)
- pandoc
- lp